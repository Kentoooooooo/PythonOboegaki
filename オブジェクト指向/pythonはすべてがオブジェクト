🔵Pythonはすべてがオブジェクト
Pythonはオブジェクト指向をサポートしている言語。すべてのデータの型がオブジェクトになっている。
文字列・数値・リスト・辞書・タプルなど、これらはすべてクラスから作られたオブジェクト。

データ型  ⇒ クラス名
文字列       class str
数値         class int
リスト       class list
辞書         class dict
タプル       class tuple

🔵オブジェクト名.関数名

メソッドを呼び出すときには、そのメソッドがそのクラスに属しているか明示する必要がある。
そこで、「オブジェクト.メソッド名」のようにドットでつなげる。

s = 'aaabbb'
s.count('a')
を例にとると変数sは文字列（str）クラスのオブジェクト。
strクラスにはcount関数が定義されており、s.countという形でメソッドを呼び出せる。

もし変数sが文字列オブジェクト出ない場合は、s.count()という形でメソッドを呼び出せない（他のcountメソッドがないオブジェクトの場合）
メソッドが呼び出せない場合はA「ttribute Error」（属性エラー）が発生する。
countなんて属性は存在しないから読みだせないよ。っていう意味

🔵組み込み関数
len関数のように、メソッドではなくimportせずに使える関数のことを組み込み関数と呼ぶ。
例えば、s.upper()のupperは、文字列クラスなので、組み込み関数とは呼ばない。
