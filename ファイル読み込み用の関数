🔵主な読み込み関数
・read        :すべての内容を文字列として返す。
・readline   ：1行を読み込んでかえす
・readlines  ：全行を読み込み、行ごとのリストとして返す。引数を指定すると指定行数だけ読み込む

🔵ファイルオブジェクトはイテラブル。
各要素は1行の内容になる。どういうことかというとfor row in f:のときみたいに
各行に対して処理が実行される。

イテラブルなのでlist(fp)は、行のリストになる。
fp.readlines()と同じですが、一度にファイルサイズのメモリを使ってしまう。
⇒ 一行ずつ処理すれば、メモリの使用量を抑えることができる。
